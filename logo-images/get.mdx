---
title: "Parameters"
---

<ParamField path="token" type="string">
  🔑 Your API token is required to be used with a whitelisted site.
</ParamField>

<ParamField path="size" type="int">
  ↔️ Change the default size of "128".
</ParamField>

<ParamField path="format" type="string">
  🖼️ Can either return "jpg" or "png".
</ParamField>

<ParamField path="greyscale" type="boolean">
  ⚫️ Optional parameter to return a desaturated image.
</ParamField>

<ParamField path="fallback" type="string">
  ⚠️ When failing to find a logo, return a default "monogram" or "404".
</ParamField>
